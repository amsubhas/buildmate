# Production reference only — never commit real secrets.
PUBLIC_URL=https://cms.buildmate.in
CORS_ORIGIN=https://buildmate.in,https://www.buildmate.in
DB_CLIENT=pg
# Prefer managed PostgreSQL and S3-compatible storage in production.
# Configure the Directus S3 storage driver with credentials supplied by your infrastructure provider.
